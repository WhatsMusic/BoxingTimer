class TrainingPlanLevel(level: String, exercises: MutableList<Exercise>) : TrainingPlan(exercises) {

    fun filterTrainingPlan(level: String) {
        // Implementieren Sie hier die Logik, um den Trainingsplan zu filtern
    }
}